const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;
const reveals=document.querySelectorAll('.reveal');
if(!reduce&&'IntersectionObserver' in window){const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.12});reveals.forEach(e=>io.observe(e));}else reveals.forEach(e=>e.classList.add('in'));
if(!reduce){document.querySelectorAll('.tilt,.tilt-card').forEach(card=>{const max=Number(card.dataset.tilt||7);card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${(-y*max).toFixed(2)}deg) rotateY(${(x*max).toFixed(2)}deg) translateZ(${card.classList.contains('tilt-card')?2:0}px)`});card.addEventListener('pointerleave',()=>{card.style.transform=card.classList.contains('tilt-card')?'':'rotate(7deg) rotateY(-10deg) rotateX(3deg) translateZ(35px)'});});}
const nav=document.querySelector('.nav');let last=0;window.addEventListener('scroll',()=>{const y=window.scrollY;if(nav)y>20?nav.classList.add('scrolled'):nav.classList.remove('scrolled');last=y},{passive:true});
